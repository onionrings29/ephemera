# Ephemera API Environment Variables
# ===================================
# Most settings are configured via the web UI setup wizard.
# Only infrastructure-level settings go here.

# FlareSolverr (for slow download fallback)
FLARESOLVERR_URL=http://localhost:8191

# Server
PORT=8286
DB_PATH=./data/database.db

# Public URL (set in production for CORS/auth)
# BASE_URL=https://ephemera.yourdomain.com

# Additional CORS origins (comma-separated, optional)
# ALLOWED_ORIGINS=https://other.domain.com

# Subpath hosting (optional)
# API_BASE_PATH=/api
# HTML_BASE_HREF=/ephemera/
